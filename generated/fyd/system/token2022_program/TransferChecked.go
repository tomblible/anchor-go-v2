// Code generated by https://github.com/gagliardetto/anchor-go. DO NOT EDIT.

package token2022_program

import (
	"bytes"
	"fmt"
	binary "github.com/gagliardetto/binary"
	solanago "github.com/gagliardetto/solana-go"
	programparser "github.com/yydsqu/solana-sdk/program_parser"
)

// Builds a "transfer_checked" instruction.

// Transfers tokens from one account to another either directly or via a
// delegate.  If this account is associated with the native mint then equal
// amounts of SOL and Tokens will be transferred to the destination
// account.
//
// This instruction differs from Transfer in that the token mint and
// decimals value is checked by the caller.  This may be useful when
// creating transactions offline or within a hardware wallet.
type TransferChecked struct {
	// Params:
	Amount   uint64
	Decimals uint8
	// Accounts:
	// [0] = [writable] source
	Source solanago.PublicKey `bin:"-"`
	// [1] = [] mint
	Mint solanago.PublicKey `bin:"-"`
	// [2] = [writable] destination
	Destination solanago.PublicKey `bin:"-"`
	// [3] = [,signer] owner
	Owner solanago.PublicKey `bin:"-"`
	// PublicKeySlice
	solanago.PublicKeySlice `bin:"-"`
}

func (obj TransferChecked) MarshalWithEncoder(encoder *binary.Encoder) (err error) {
	// Serialize `amountParam`:
	if err = encoder.Encode(obj.Amount); err != nil {
		return fmt.Errorf("error while marshaling amountParam:%w", err)
	}
	// Serialize `decimalsParam`:
	if err = encoder.Encode(obj.Decimals); err != nil {
		return fmt.Errorf("error while marshaling decimalsParam:%w", err)
	}
	return nil
}

func (obj *TransferChecked) UnmarshalWithDecoder(decoder *binary.Decoder) (err error) {
	// Deserialize `Amount`:
	if err = decoder.Decode(&obj.Amount); err != nil {
		return fmt.Errorf("error while unmarshaling Amount:%w", err)
	}
	// Deserialize `Decimals`:
	if err = decoder.Decode(&obj.Decimals); err != nil {
		return fmt.Errorf("error while unmarshaling Decimals:%w", err)
	}
	return nil
}

func (obj *TransferChecked) SetAccounts(accounts solanago.PublicKeySlice) (err error) {
	if len(accounts) < 4 {
		return fmt.Errorf("too few accounts, expect %d actual %d", 4, len(accounts))
	}
	obj.Source = accounts[0]
	obj.Mint = accounts[1]
	obj.Destination = accounts[2]
	obj.Owner = accounts[3]
	obj.PublicKeySlice = accounts
	return nil
}

func (obj *TransferChecked) Accounts() solanago.PublicKeySlice {
	return obj.PublicKeySlice
}

func (obj *TransferChecked) SignerAccounts() solanago.PublicKeySlice {
	return solanago.PublicKeySlice{obj.Owner}
}

func (obj *TransferChecked) PublicKeys() solanago.PublicKeySlice {
	return obj.PublicKeySlice
}

func (*TransferChecked) TypeID() binary.TypeID {
	return binary.TypeIDFromBytes(Instruction_TransferChecked)
}

func (*TransferChecked) NewInstance() programparser.Instruction {
	return new(TransferChecked)
}

// Builds a "transfer_checked" instruction.
// Transfers tokens from one account to another either directly or via a // delegate.  If this account is associated with the native mint then equal // amounts of SOL and Tokens will be transferred to the destination // account. //  // This instruction differs from Transfer in that the token mint and // decimals value is checked by the caller.  This may be useful when // creating transactions offline or within a hardware wallet.
func NewTransferCheckedInstruction(
	// Params:
	amountParam uint64,
	decimalsParam uint8,

	// Accounts:
	source solanago.PublicKey,
	mint solanago.PublicKey,
	destination solanago.PublicKey,
	owner solanago.PublicKey,
	remaining__ ...*solanago.AccountMeta,
) (*solanago.GenericInstruction, error) {
	var (
		err    error
		buf__  = new(bytes.Buffer)
		enc__  = binary.NewBorshEncoder(buf__)
		metas_ = make(solanago.AccountMetaSlice, 4, 4+len(remaining__))
	)

	// Encode the instruction discriminator.
	if err = enc__.WriteBytes(Instruction_TransferChecked[:], false); err != nil {
		return nil, fmt.Errorf("failed to write instruction discriminator: %w", err)
	}

	{
		// Serialize `amountParam`:
		if err = enc__.Encode(amountParam); err != nil {
			return nil, fmt.Errorf("error while marshaling amountParam:%w", err)
		}
		// Serialize `decimalsParam`:
		if err = enc__.Encode(decimalsParam); err != nil {
			return nil, fmt.Errorf("error while marshaling decimalsParam:%w", err)
		}
	}

	// Add the accounts to the instruction.
	{
		// [0] = [writable] source
		// The source account.
		metas_[0] = solanago.NewAccountMeta(source, true, false)
		// [1] = [] mint
		// The token mint.
		metas_[1] = solanago.NewAccountMeta(mint, false, false)
		// [2] = [writable] destination
		// The destination account.
		metas_[2] = solanago.NewAccountMeta(destination, true, false)
		// [3] = [,signer] owner
		// The source account's owner/delegate.
		metas_[3] = solanago.NewAccountMeta(owner, false, true)
		// append remaining metas
		metas_ = append(metas_, remaining__...)
	}

	// Create the instruction.
	return solanago.NewInstruction(
		ProgramID,
		metas_,
		buf__.Bytes(),
	), nil
}

// Builds a "BuildTransferChecked" instruction.
// Transfers tokens from one account to another either directly or via a // delegate.  If this account is associated with the native mint then equal // amounts of SOL and Tokens will be transferred to the destination // account. //  // This instruction differs from Transfer in that the token mint and // decimals value is checked by the caller.  This may be useful when // creating transactions offline or within a hardware wallet.
func BuildTransferChecked(
	// Params:
	amountParam uint64,
	decimalsParam uint8,

	// Accounts:
	source solanago.PublicKey,
	mint solanago.PublicKey,
	destination solanago.PublicKey,
	owner solanago.PublicKey,
	remaining__ ...*solanago.AccountMeta,
) *solanago.GenericInstruction {
	instruction_, _ := NewTransferCheckedInstruction(
		amountParam,
		decimalsParam,
		source,
		mint,
		destination,
		owner,
		remaining__...,
	)
	return instruction_
}
